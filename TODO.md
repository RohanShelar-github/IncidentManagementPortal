- [ ] Inspect `js/app.js` `_drawTrend` implementation and chart expectations
- [ ] Add UI toggle for trend aggregation (monthly vs weekly)
- [ ] Replace `_drawTrend` with real data bucketing from `incidents[].date`
- [ ] Ensure `initCharts()` passes current filters and re-renders correctly
- [ ] Test by running the portal and verifying trend updates with dashboard filters

