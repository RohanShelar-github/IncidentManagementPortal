# Runtime secrets stay on the server and must never be committed.
backend/logs/

# Runtime dependencies and generated operational data are restored or recreated locally.
backend/node_modules/
node_modules/
